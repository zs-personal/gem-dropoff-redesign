# Pin the public registry. Without this, a machine configured against a private
# mirror bakes internal URLs into package-lock.json, which then hangs any CI
# runner that cannot reach that host.
registry=https://registry.npmjs.org
